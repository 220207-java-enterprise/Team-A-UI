import { Principal } from "../models/principal";


interface IDashboardProps {
    currentUser: Principal | undefined
}

function FMTable(props: IDashboardProps) {
    // useEffect(() => {
    //     getAllReimbursements().then(resp => {
    //             console.log(resp)
    //             setReimbursments(resp.data)
    //         });
    // }, []
    // );
}